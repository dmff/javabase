synchronized关键字：
    1.普通方法锁定this对象，static方法锁定当前class的类对象，
           同步块里面可以锁定其他对象
    2.理解多线程的原理是：CPU片段的执行
    3.同步方法和非同步方法是可以同时调用
    4.对业务的写加锁，读操作不加锁，容易产生脏读(读到还没有写入的数据)
    5.同步方法可以调用其他同步方法，说白了是可以重入的(比如子类重写调用父类的同步方法)
    6.代码执行完或者抛出异常会释放对象锁，需要谨慎对待异常抛出